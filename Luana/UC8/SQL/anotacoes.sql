

use nome do banco; // selecinar tabela
SELECT * FROM nome da "tabela" // verficar se a tabela foi criada


Insert into `nome da tabela` (//nome das colunas 'exemplo', 'exemplo2') VALUES
(//dados a ser inserido 'exemplo', 'exemplo2')

SELECT * FROM senac_cliente.tb_clientes; #selecionar toda a tabela.
SELECT * FROM tb_clientes; #selecionar toda a tabela.
SELECT * FROM tb_clientes where id_cliente = 5; #localizar um iten na tabela.
Select nome_cliente from tb_clientes; # para localizar uma coluna.
Select * from tb_clientes order by nome_cliente asc; # para localizar uma coluna.

Drop table clientes;
